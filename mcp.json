{
  "mcpServers": {
    "datadog": {
      "type": "http",
      "url": "https://api.us3.datadoghq.com/api/v1/mcp",
      "headers": {
        "DD_API_KEY": "${DD_API_KEY}",
        "DD_APPLICATION_KEY": "${DD_APP_KEY}"
      }
    }
  }
}
